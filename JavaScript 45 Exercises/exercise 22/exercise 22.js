const fruits = ["Banana", "Apple", "Mango"];

/*when we ask for unasinged index, value is undefined. So, the log output will be undefined. JavaScript does 
not throw an index error because of this. */

console.log(fruits[4]);
